<div id="<%= dom_id blog %>">
  <p>
    <strong>Title:</strong>
    <%= blog.title %>
  </p>
  <td> Published <%= distance_of_time_in_words(blog.created_at, Time.now)%> ago </td>
  <p>
    <strong>Body:</strong>
    <%= blog.body %>
  </p>
</div>
